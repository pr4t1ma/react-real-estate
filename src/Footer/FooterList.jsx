export const FooterList = ({ links, title }) => (
  <div className="">
    <h3>{title}</h3>
    <ul>
      {links.map((link) => (
        <li>
          <a href="" />
        </li>
      ))}
    </ul>
  </div>
);
